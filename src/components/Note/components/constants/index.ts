import { TabType } from 'types';

export const tagColors: { [key in TabType]: string } = {
  all: '#D2E0F4',
  checked: '#BFFABD',
  critical: '#FCBABA',
  hr: '#fff',
  info: '#fff',
  it: '#fff',
};
